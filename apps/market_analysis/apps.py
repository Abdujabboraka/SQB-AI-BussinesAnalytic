from django.apps import AppConfig

class MarketAnalysisConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.market_analysis'
    verbose_name = 'A Bloki — Bozor tahlili'
