from django.apps import AppConfig

class CompetitionRiskConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.competition_risk'
    verbose_name = 'E Bloki — Raqobat va risklar'
