from django.apps import AppConfig

class DemandForecastConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.demand_forecast'
    verbose_name = "B Bloki — Talab prognozi"
